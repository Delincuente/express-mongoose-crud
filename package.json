{
  "name": "express-mongoose-crud",
  "version": "1.0.0",
  "description": "This repository demonstrates a complete CRUD (Create, Read, Update, Delete) operation setup using Node.js + Express + Router + dotenv + express-validator + Mongoose. It also includes a Global Error Handler with 404 Not Found handling.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}